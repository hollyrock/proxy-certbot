# Proxy-Certbot

Docker components to run nginx with certbot.
This site environment made for Django + Nginx + uwsgi + Certbot application.

## Consiguration

You will need to modify proxy/default_sample.conf to fit your environment, and change file name to default_prod.conf.
